
/** Optional: only needed if you host in-browser on the portal domain. */
window.ZOOM_CFG = {
  SDK_KEY: "YOUR_ZOOM_SDK_KEY",
  MEETING_NUMBER: "YOUR_MEETING_NUMBER",
  PASSCODE: "YOUR_MEETING_PASSCODE"
};
