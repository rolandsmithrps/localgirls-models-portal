/** Edit these if hosting via the portal domain (models.localgirls.io). */
window.ZOOM_CFG = {
  SDK_KEY: "YOUR_CLIENT_ID",
  MEETING_NUMBER: "YOUR_MEETING_NUMBER",
  PASSCODE: "YOUR_MEETING_PASSCODE"
};
